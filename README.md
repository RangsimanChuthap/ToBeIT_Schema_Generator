# ToBeIT_Schema_Generator
Parse .csv files and generate data schema for constructring &lt;g>learn account.

## How to use
1. Run the program
2. Select files using explorer windows
3. The constructor schema will be generated at schema path configuration (default: SCHEMA.csv)
